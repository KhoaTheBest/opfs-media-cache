export const ROOT_PATH = 'pencil-opfs-storage';
export const ASSETS_PATH = `${ROOT_PATH}/assets`;
export const METADATA_FILE = `${ROOT_PATH}/metadata.json`;

export const MAX_STORAGE_SIZE = 1000 * 1024 * 1024; //1000 MB
export const CHUNK_SIZE = 1024 * 1024; //1 MB